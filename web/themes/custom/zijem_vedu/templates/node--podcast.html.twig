{{ attach_library('zijem_vedu/castos-player') }}
      <div class="container">
        <div class="row">
          <div class="col-12 col-xl-8 offset-xl-2">
            <div class="d-flex justify-content-center align-items-center py-3">
              <div class="article__tag mb-0 color-gold">Podcast: {{ node.field_podcast.entity.label }}&nbsp;<i class="fas fa-headphones-alt"></i></div>
              <div class="fs-7 ml-1">{{ node.created.value|date('d. m. Y') }}</div>
            </div>
            <figure class="figure">
              <img src="{{ file_url(node.field_podcast.entity.field_header_obrazok.entity.uri.value) }}" class="figure-img img-fluid rounded" alt="Podcast {{ node.field_podcast.entity.label }}">
            </figure>
          </div>
          <div class="col-12 col-xl-6 offset-xl-3">
            {% include '@zijem_vedu/podcast-player.html.twig' with { node } %}
            <?php endif; ?>
            <div class="podcast-description">
              {{ node.field_podcast.entity.field_o_podcaste.value | raw }}
            </div>
            {{ node.body.value|raw }}
          </div>
          <div class="col-12 col-xl-6 offset-xl-3">
            {{ content.field_popis }}
            {{ content.field_galeria }}
            {{ content.field_tags }}
          </div>
        </div>
      </div>
